"""
Code vendored from the T-ITS 2025 reference codebase
(Graph-Scene-Representation-and-Prediction, READ-ONLY, sibling directory).

Copied rather than imported so this repo has no runtime dependency on that
repo's location or state. Trim to what st_gat/pipeline actually uses:
Point, GraphBuilder, and a minimal MapProcessor.
"""
