Framing note for presenting this slide: lead time is supporting evidence for the calibrated-confidence claim, not the headline result on its own. Early Arm A testing showed Autoware's own MRM already reacts fast once something is sufficiently wrong, so a single "we beat MRM by N seconds" number has limited independent value — the actual point of the lead-time computation is the SHAPE of the curve (how lead time trades off against fault severity and against the calibration guarantee's tightness), which is a genuinely different kind of result than a single number, and it doesn't restrict the dissertation from moving beyond detection the way an isolated headline metric would.

Honest caveats to have ready, all already flagged in the artifacts themselves, not hidden: (1) even after tightening, achievable alpha is ~0.025-0.028, not the requested 0.1 — used 0.1 anyway since it's comfortably inside what's achievable now, but the guarantee's tightness is still gated by how many zone-tagged nominal trials exist; (2) the only fatal-moment ground truth right now is Arm A's own metrics.json heuristic — self-referential until Arm B data exists; (3) looking at the representative trace directly, this trial's residual signal actually crosses the threshold well before the zone-entry window the script currently searches from — meaning the reported ~102s lead time is a CONSERVATIVE lower bound, the true number is likely larger.

The discriminability refinement (negative-evidence subtype, not "TL faults in general") is a genuine finding worth defending on its own terms, but per Kalpit's own read: it's a sharpening of an existing claim, not a new contribution — good supporting material for the paper/dissertation background, not something to lead with as new research.

MAJOR UPDATE 2026-08-06 — this slide's framing is now superseded and needs a real rework, not just new figures bolted on. Two things changed:

(1) CUSUM (representative_cusum_trace.png) is replaced in the codebase by an SPRT/sequential-evidence statistic (Page/Lorden reset-at-zero accumulator over a variance-inflation log-likelihood-ratio — see st_gat/residuals.py's module comments for the full derivation and numerical verification). The headline property: p_fault_* = sigmoid(accumulated log-likelihood-ratio) is a genuine running probability, not a raw magnitude that only means something once you pick an external threshold — mathematically a real improvement over CUSUM. CUSUM columns are kept in the traces for comparison, not deleted.

(2) But this whole slide's detection-rate/lead-time framing is exactly what the 2026-08-06 reframe walked back from as the wrong lens — see CLAUDE.md's "Direction reframe" section. Two new figures (nominal_floor.png, fault_reaction_grid.png) check the SPRT signal the way the reframe actually asks: does it stay quiet under nominal noise, does its shape react sensibly to a fault. Answer right now: no — it saturates to >0.99 confidence roughly every 9 seconds during ordinary NOMINAL driving with no fault present (verified numerically: 92% of nominal time at floor, but 17.6 full reset-to-saturation events per trial on average). Root-caused, not just observed: 96% of that is the traffic_light_discrepancy Bernoulli branch alone — its ~9.3%-per-10Hz-step nominal positive rate is too high for a memoryless sequential test to treat as rare evidence, so it saturates almost continuously regardless of fault state. This is the direct downstream consequence of slide 10's calibration finding, not a separate bug.

Recommended honest story for this update, in order: model's own uncertainty was measurably miscalibrated in a way a single std(z) number hid (slide 10) → that miscalibration is the specific, traced root cause of why the new (mathematically sounder) SPRT signal doesn't yet behave usably (this slide) → fixing calibration is the concrete next step, and the detection-rate/lead-time numbers on this slide should NOT be presented as current results until that's done — they were computed on the pre-SPRT CUSUM statistic under the old framing anyway. Consider whether this slide needs to become two slides (the calibration-cascades-into-signal-behavior story, then whatever replaces the old detection-rate table) rather than patching the existing one — that's a real open call for you, not decided here.
