This is the single most important slide to get across cleanly: detection is NOT what's being defended anymore — it's the given, banked result from the published paper. The three new axes (calibrated confidence, lead time, severity) are what make this a dissertation-level contribution rather than a re-run of the paper with a different model. Be ready to state directly: "if I only re-ran detection and got a similar or better accuracy number, that would be incremental — this chapter's job is to answer questions the accuracy number can't."

Arm B not yet being collected is an honest limitation, not hidden: early testing showed Autoware's own MRM (minimum risk maneuver) already triggers an emergency stop once it detects something is sufficiently wrong, which is Arm B's actual ground-truth signal. Right now all reported lead-time numbers use Arm A's own metrics.json heuristic (permanent_stop_time_s / likely_static_collision) as a self-referential fatal-moment anchor — flagged explicitly in every result, not glossed over. The mrm_first_trigger_rel_s column is already being carried through the pipeline so Arm B data can slot in as an independent oracle later without reworking anything.
