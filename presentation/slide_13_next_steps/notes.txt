The "no new data collection" constraint is real and drove this entire session's approach — every result on this deck's slides 11-12 comes from either already-collected data or analysis-only extensions of existing data (the zone-coverage expansion), deliberately avoiding anything that requires a new AWSIM/Autoware run. Good to state this plainly if asked why the plan looks the way it does.

The active-rectification idea is worth presenting honestly as exploratory and NOT yet a commitment — the feasibility note is genuinely a "should we reopen this scope" input, not a decision. Central risk to be ready to explain if asked: the model would need to be trusted most exactly when it's least in-distribution (faults are rare relative to nominal training data), which is a materially harder bar than the current passive-alarm claim. The one cheap thing worth trying before committing further: an offline counterfactual replay using already-collected paired (fault trial, same-goal nominal trial) data — no new experiments needed for that first check either.

The two advisor questions are genuine, not rhetorical — particularly the first: whether a committee expects a specific lead-time-vs-MRM number is a real scoping question that affects how much effort goes into extending the CUSUM search (next-steps item 2) vs. treating the current lead-time numbers as sufficient supporting evidence.
