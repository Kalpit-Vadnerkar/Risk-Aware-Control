NEXT STEPS

No new data collection for the near term (lab access constraint) — so the
immediate plan is entirely methodology and analysis refinement on existing data:

  1. Severity/dose-response axis: use the fault-intensity variants already
     collected (s1->s4, ramp, scale/stuck) to plot lead time and detection
     rate against severity, not just per-campaign averages

  2. Tighten the CUSUM lead-time search: the representative trace (slide 11)
     shows the true first-crossing likely happens before the zone-entry
     window currently searched — extending the search to the full trial
     (with care not to credit spurious pre-window blips) could recover a
     meaningfully larger, more accurate lead-time number

  3. Per-feature VAR_FLOOR and route/goal-signal dilution fix (both logged
     in docs/research_notes/model_improvement_notes_2026.md, not yet done)

  4. Deep ensemble for epistemic uncertainty — matters specifically for
     trusting calibration under the distribution shift a fault represents

Open data gap, not solvable without new collection:
  → Zero TL fault trials have reached a real fatal moment yet — no TL
    lead-time result is possible until either more severe TL fault
    parameters or more repeats produce a genuine dangerous outcome

Longer-term / stretch, scope question for discussion:
  → Explored this session (docs/research_notes/active_rectification_feasibility_2026.md,
    literature review only, no code): using the model's own map-grounded
    expectation not just to alarm, but to actively substitute a corrected
    value back into the interceptor when a fault is detected — a materially
    different research bet (new training objective, closed-loop evaluation,
    harder certification argument) than the current scoped contribution

Questions for advisor:
  → Is reframing lead time as supporting evidence (not the headline) the
    right call, or does the committee expect a lead-time-vs-MRM comparison
    specifically?
  → Worth pursuing the active-rectification stretch goal at all this cycle,
    or keep it purely as future work?
