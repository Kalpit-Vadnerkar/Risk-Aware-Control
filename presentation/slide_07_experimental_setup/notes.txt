Mostly unchanged from the last presentation — platform choice hasn't
moved. The one addition worth calling out if there's time: the zone
geometry (turn_zones.json/tl_zones.json) originally built purely for
fault-injection gating turned out to be exactly the right taxonomy for
this cycle's calibration auditing too — a real example of infrastructure
paying off twice, not planned that way originally.

The old Arm A/B (safety-gate-disabled vs. stock) toggle from prior
presentations is not a focus of this update — nothing this cycle depended
on it, and it isn't mentioned elsewhere in this deck. Don't bring it up
unless asked; if asked, it's still available infrastructure, just not
exercised this cycle since the current claim doesn't need a lead-time-vs-
MRM comparison.
