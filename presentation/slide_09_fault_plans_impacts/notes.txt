This slide's content and plots are unchanged from the prior deck — the
message-level verification step didn't need to change with the
calibration pivot, it's a lower-level check that everything else depends
on regardless of which uncertainty framework sits on top of it.

The explicit "necessary but not sufficient" closing line is new and
important — it's the bridge to slide 13, and it preempts the obvious
follow-up question ("so does the fault actually matter to the model")
before it's asked here prematurely; slide 13 is where that's actually
answered, with real numbers.

If asked for a concrete example of the "fault ran but never reached the
signal" failure mode mentioned: an early TL fault campaign subscribed to
a topic Autoware never actually publishes (assumed a raw/final split that
doesn't exist in this Autoware install) — every trial completed
normally, fault_log.jsonl looked fine, and the injected fault reached
zero real messages the whole time. Found only by this exact
message-level check, not by anything at the trial-outcome level.
