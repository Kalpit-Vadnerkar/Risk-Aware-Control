DATA COLLECTED SO FAR

Nominal baseline (nom_v11): 26 goals across the Nishishinjuku map, ~2 trials
each — the reference distribution everything else is compared against.
(Plot: route_map_example_goal012.png — one goal's route over the HD map,
traffic-light zones marked)

Fault campaigns (8 total, run on 3 representative goals: 007, 012, 026):

  IMU faults                        | Traffic-light faults
  -----------------------------------|-----------------------------------
  imu_fault_s1  — mild bias          | tl_fault_s2 — oscillating color
    (deliberate negative control,    | tl_fault_s3 — UNKNOWN classification
     designed to be harmless)        |   (negative-evidence case)
  imu_fault_s3  — stronger bias      | tl_fault_s4 — signal blackout
  imu_fault_scale — gyro scale error |   (negative-evidence case)
  imu_fault_stuck — gyro frozen      | tl_fault_ramp — confidence decays
                                     |   continuously to zero

All fault campaigns gate on a route-derived zone (turn zone, bias lead-in
zone, or traffic-light zone) computed from the map + planned route — not a
live threshold or a wall-clock guess.

This session's addition: zone files (turn_zones.json, tl_zones.json) were
extended from 3 goals to all 26 nominal goals — analysis-only, reusing
already-collected rosbags, no new experiments — to grow the nominal
calibration sample for Stage 4 (see slide 11).
