# External dependencies (live in parent dir /home/kvadner/Desktop/Dissertation/, not inside this repo)
# autoware/          → /home/kvadner/Desktop/Dissertation/autoware/
# awsim_labs_v1.6.1/ → /home/kvadner/Desktop/Dissertation/awsim_labs_v1.6.1/
# Map/               → /home/kvadner/Desktop/Dissertation/Map/nishishinjuku_autoware_map/

# Python caches
__pycache__/
*.pyc
*.pyo

# Python venv (torch/CUDA + networkx/tqdm for st_gat, --system-site-packages so ROS stays visible)
.venv/

# Experiment data (large rosbags)
experiments/data/

# ST-GAT extracted/windowed sequence cache (regenerable from experiments/data/
# via `python3 -m st_gat.pipeline.run_pipeline`; grows large — one horizon-tagged
# subtree per INPUT_SEQ_LEN/OUTPUT_SEQ_LEN combo, see cfg.HORIZON_TAG)
st_gat/data/

# Downloaded papers
docs/papers/

# Background job logs (systemd-run --user output redirects here for
# multi-hour training/experiment jobs, see feedback_background_process_survival
# memory) — operational output, not a data artifact worth tracking
logs/

# Per-window fault-inspection data (2026-08-27): regenerable by re-running
# inspect_fault_predictions.py, grows every time a field is added (44,908
# rows -- one per calibration window across every fault trial), already
# past 68MB and climbing -- tripped GitHub's 50MB size warning twice.
# Not meaningful to diff or review in git history; the trace PNGs and the
# printed aggregate summary (in commit messages / research notes) are the
# things worth keeping under version control, not this file.
experiments/analysis/fault_prediction_inspection/fault_prediction_summary.json

# OS and editor files
.DS_Store
.vscode/
.idea/
*~
*.swp
*.swo
